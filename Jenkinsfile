pipeline {
    agent any

    stages {
          stages('checkout code'){
              steps{
              checkout scm
              sh "pwd"
              sh"ls -lrt"
              }
          }
       
