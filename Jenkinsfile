pipeline {
    agent any

    stages {
          stage('checkout code'){
              steps{
              checkout scm
              sh "pwd"
              sh"ls -lrt"
              }
          }
    }
}
